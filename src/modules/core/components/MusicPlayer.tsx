// Expo

import { Text, View } from "react-native";

// React

// React Native

// Hooks

// Definitions

// Components

// Props

// Api

export default function MusicPlayer() {
  return (
    <View
      style={{
        position: "absolute",
        top: 0,
        zIndex: 2,
        backgroundColor: "red",
        width: "100%",
      }}
    >
      <Text>jajaja</Text>
    </View>
  );
}

// const styles = StyleSheet.create({

// });
